{
  "name": "signup_cash_core",
  "version": "0.8.0",
  "description": "A non-custodial easy-to-use platform to build DApps on top of Bitcoin Cash blockchain",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"lerna run build:dev --stream --profile\"  \"lerna run start\"",
    "build:dev": "lerna run build:dev",
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bch",
    "bitcoin",
    "bitcoin",
    "cash",
    "blockchain",
    "javascript",
    "library",
    "crypto"
  ],
  "author": "p0o",
  "license": "MPL-2.0",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "lerna": "^3.22.1",
    "serve": "^11.3.2",
    "webpack-cli": "^4.6.0"
  }
}
